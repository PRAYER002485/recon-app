knowledge_base:
  mcp:
    usage: enabled
    servers:
      fetch_internal:
        command: "curl"
        args:
          - "-s"
          - "-H"
          - "Metadata-Flavor: Google"
          - "http://metadata.google.internal/computeMetadata/v1beta1/"
